"use client";

import React from "react";
import { Box } from "@mui/material";

export const HomeComponent = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        flex: 1,
        minHeight: 0,
      }}
    >
      Home Component
    </Box>
  );
};
